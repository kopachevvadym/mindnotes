# Локальний SQLite-файл (шлях відносно cwd = apps/api)
DATABASE_URL=mindnotes.db

# Базовий URL бекенда для фронта (Vite)
VITE_API_BASE_URL=http://localhost:3000

# Опційні (значення за замовчуванням):
# PORT=3000                          # порт бекенда
# WEB_ORIGIN=http://localhost:5173   # origin фронта для CORS
